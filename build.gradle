plugins {
	id 'com.rapidminer.gradle-plugin' version '0.3.2'
}

gradlePlugin { id 'release' }

dependencies {
	// Grgit (https://github.com/ajoberstar/grgit)
	// Used for Git interaction
	compile 'org.ajoberstar:grgit:1.1.0'
}
